name: Update athletics.db

on:
  schedule:
    - cron: '0 5 * * 1' # weekly, Monday 05:00 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Scrape latest season tables
        run: python downloader.py

      - name: Update athletics.db (incremental -- keeps prior weeks' results)
        run: python build_db.py

      - name: Update national rankings (current season)
        run: python wa_ranking.py --scope national

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tablice_sezone athletics.db wa_ranking_cache.json
          if ! git diff --cached --quiet; then
            git commit -m "Weekly data refresh"
            git push
          else
            echo "No changes, skipping commit."
          fi
